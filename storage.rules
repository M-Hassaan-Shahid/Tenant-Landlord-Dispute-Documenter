rules_version = '2';

// ProofNest stores media on Cloudinary, not Firebase Storage.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
