rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /photos/{propertyId}/{itemId}/{photoId} {
      allow read, write: if request.auth != null;
    }

    match /signatures/{propertyId}/{signatureId} {
      allow read, write: if request.auth != null;
    }
  }
}
