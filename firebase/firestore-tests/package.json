{
  "name": "proofnest-firestore-tests",
  "version": "1.0.0",
  "description": "Firestore security-rules tests, run against the local emulator (free tier).",
  "private": true,
  "scripts": {
    "test": "node --test"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.12.2"
  }
}
