package com.example.tenant_landlorddisputedocumenter.data.remote

/**
 * Public Cloudinary identifiers. Upload signatures are minted by Cloud Functions so the
 * API secret never ships in the APK.
 */
object CloudinaryConfig {
    const val CLOUD_NAME = "dspdmxdaq"
}
